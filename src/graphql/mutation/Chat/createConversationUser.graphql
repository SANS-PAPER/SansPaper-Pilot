mutation AddUsersToConversation($input: CreateConversationUserInput!) {
  createConversationUser(input: $input) {
    conversationUser {
      id
      createdAt
      updatedAt
      conversationId
    }
  }
}
